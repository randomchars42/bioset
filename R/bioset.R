#' Convert a matrix of raw values into nice and tidy data.
#'
#' [as.package()] is intended to save you from commons tasks when dealing with
#' raw data obtained e.g. from a measuring device.
#'
#' When importing values with [as.package()] start with [set_read()]. To
#' get an example for a workflow refer to the vignettes.
"_PACKAGE"
